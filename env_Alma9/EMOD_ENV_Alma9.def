Bootstrap: docker
From: almalinux:9.0

%post
    dnf upgrade -y

    dnf -y install python
    dnf -y install python-pip
    dnf -y install mpich

    dnf clean all

    pip install pip              --upgrade
    pip install emod-api==1.25.1 --extra-index-url https://packages.idmod.org/api/pypi/pypi-production/simple
    pip install scikit-learn

%runscript


%environment


%test


%labels
    Author kfrey@idmod.org

%help
    Minimal container for running EMOD using emod-api for file support.
Bootstrap: library
From: centos:8.4

%post
    dnf install python3 -y
    dnf install snappy -y
    dnf install mpich -y
    dnf clean all

    python3 -m pip install pip --upgrade
    python3 -m pip install emod-api==1.17.2 --extra-index-url https://packages.idmod.org/api/pypi/pypi-production/simple

%runscript


%environment
    export PATH=$PATH:/usr/lib64/mpich/bin
    export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/lib64/mpich/lib

%test


%labels
    Author kfrey@idmod.org

%help
    Minimal container for running EMOD using emod-api for file support.
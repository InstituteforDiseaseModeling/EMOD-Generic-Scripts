Bootstrap: docker
From: almalinux:8.5

%post
    dnf -y install python39
    dnf -y install mpich
    dnf clean all

    python3 -m pip install pip              --upgrade
    python3 -m pip install emod-api==1.22.2 --extra-index-url https://packages.idmod.org/api/pypi/pypi-production/simple

%runscript


%environment


%test


%labels
    Author kfrey@idmod.org

%help
    Minimal container for running EMOD using emod-api for file support.